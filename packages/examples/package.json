{
  "name": "examples",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "oxlint --deny-warnings",
    "lint:fix": "bun run lint --fix",
    "fmt": "oxfmt --check",
    "fmt:fix": "oxfmt",
    "fix": "cd ../.. && bun run fix"
  },
  "dependencies": {
    "@superlibdotdev/superlib": "workspace:*",
    "remeda": "^2.32.0",
    "zod": "^4.3.5"
  }
}
